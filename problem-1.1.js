// Write an arrow function that will take 3 parameters, will multiply
// the parameters and will return the result.

const multiplacation = (num1, num2, num3) => num1 * num2 * num3;
console.log(multiplacation(2, 2, 2))